# ─── Data Provider ──────────────────────────────────────────────────────────
# Use "demo" (default) or "external"
STATIONS_PROVIDER=demo

# ─── External Provider (only used when STATIONS_PROVIDER=external) ──────────
# Base URL of your external EV-stations API
# Expected response: { stations: Station[] } or Station[]
EXTERNAL_STATIONS_API_URL=https://api.example.com/v1/stations

# Optional API key sent as Bearer token in Authorization header
EXTERNAL_STATIONS_API_KEY=your_api_key_here
